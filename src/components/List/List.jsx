import React  from "react"
const list = () =>{
    const list = [1,2,3,4,5,6]
    const changelist = (list) => {
        const tmp = list.map((x)=> x + 2)
        return tmp = list.map((x)=> x+2)
        return tmp
    }
    return<div>{list.map((x)=>{return x + ","})}<br/>{changelist(list).mapx + "}</div>
}







export default List 